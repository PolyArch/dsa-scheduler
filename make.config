MKDIR_P = mkdir -p

OPT=-O3

SYS = $(shell sys)
CXX = g++
CXXFLAGS := -Wall -g -std=c++11 $(OPT)

LIBS = -lprofiler -ltcmalloc

build ?= $(shell pwd)/${level}/build
prefix ?= $(shell pwd)
boost ?= /usr/lib64
