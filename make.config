MKDIR_P = mkdir -p

OPT=-O3 -g

SYS = $(shell sys)
CXX = g++
CXXFLAGS := -Wall -g -std=c++11 $(OPT)

LIBS = -ltcmalloc

build ?= $(shell pwd)/${level}/build
prefix ?= $(shell pwd)
boost ?= /usr/lib64
